<% if signed_in?(@user) %>
	<h1> Change password for <%=@user.username%> </h1>
<% else %>
	<h1>Editing user</h1>
<% end %>

<%= render 'form' %>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>
