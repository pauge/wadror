<h1> <%=@beer.name %> </h1>

<p id="notice"><%= notice %></p>

<p>
  <b>Style:</b>
  <%= @beer.style.name %>
</p>

<p>
  <b>Brewery:</b>
  <%= @beer.brewery_id %>
</p>

<p>
<% if @beer.ratings.empty? %>
  This beer has no ratings

<% else %>

This beer has <%= pluralize(@beer.ratings.count, 'rating') %> . With the average of
<%= @beer.average_rating %>
<% end %>

<h4> Give a rating: </h4>
	<%= form_for(@rating) do |f| %>
		<%=f.hidden_field :beer_id %>
		score: <%=f.number_field :score %>
		<%= f.submit %>
	<% end %>
</p>

<%= link_to('Edit', edit_beer_path(@beer), :class => 'btn btn-primary') if current_user %> |
<%= link_to 'Back', beers_path, :class => 'btn btn-primary' %>
<%= link_to 'Destroy', @beer, method: :delete, data: { confirm: 'Are you sure?' }, :class => 'btn btn-danger'%>
